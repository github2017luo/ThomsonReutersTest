<aura:component implements="force:appHostable,flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId,force:lightningQuickActionWithoutHeader" 
                controller="LTGS_Custom_Settings_Url" access="global">
	
     <!--<aura:attribute name="recordError" type="String" access="private"/>
    <aura:attribute name="Oppty" type="Opportunity" access="public"/>-->
    
    <aura:handler name="init" action="{!c.doInit}" value="{!this}" />-->
    
    <ltng:require styles="{!$Resource.multilineToastCSS}" />
    
    <aura:html tag="style">
    .slds-backdrop {
    background: rgba(43, 40, 38, 0) !important;  
    }
    .slds-fade-in-open {
    display:none !important;
   }
</aura:html>
    
    <!-- <force:recordData aura:id="opportunityRecordLoader"
                      mode="VIEW"
                      recordId="{!v.recordId}"
                      fields="Id,Name,OpportunityID18__c"
                      targetFields="{!v.Oppty}"
                      targetError="{!v.recordError}"
                      recordUpdated="{!c.recordUpdated}"
                      />-->
    
    
</aura:component>
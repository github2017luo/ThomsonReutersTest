global class nitroScheduledRolesToGroups implements Schedulable{
   global void execute(SchedulableContext SC) {
   /* skg fix for Sept release commented out code
        NitroRolesToGroups.transferData();
        */
   }
}